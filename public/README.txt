This directory is intentionally almost empty.

The website has no static assets. Every byte a visitor receives — markup,
styling, status codes — is produced by the model at request time and served by
functions/[[path]].ts. Cloudflare Pages requires a build output directory, so
this file exists to give it one.
